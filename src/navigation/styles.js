import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  topSidebarStyle: {
    height: '30%',
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#fff'
  },
  sidebarImage: { height: 100, width: 100 }
});
